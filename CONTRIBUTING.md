# Contributing

- Follow existing code style.
- Run tests: `cd backend && npm test`
- Add unit tests for new services and controllers.
- Open PRs against `main` and include a description of changes.

